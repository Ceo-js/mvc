<!DOCTYPE html>
<html>
  <head>
    <title><%= pageTitle %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
        product-details {
            display: grid;
            grid-template-columns: repeat( 2, .5fr );
            width: 90%;
            margin: 0 auto;
            border-radius: 1.5rem;
            padding: 1.5rem;
            background: #f5f5f5;
        }

        left {
            display: block;
            width: 100%;
            aspect-ratio: 1 / 1;
        }
        left > img {
            width: 100%;
        }

        right {
            display: grid;
            grid-template-rows: repeat( 4, .4fr .2fr .3fr .1fr );
            padding: 1.5rem;
            margin: 0 auto;
        }

        right button {
            border: transparent;
            background: #905;
            padding: 1rem 1.5rem;
        }
    </style>
  </head>
  <body>
    <%- include( "./includes/appbar0" ) %>
    <main>
        <h1>Product Details</h1>

        <product-details>
            <left>
                <img src="<%= product.image %>" alt="">
            </left>
            <right>
                <h1><%= product.title %></h1>
                <p><%= product.price %></p>
                <p details><%= product.description %></p>
                <p>
                    <%- include( "includes/cartButton" ) %>
                </p>
            </right>
        </product-details>


        productDetails
    </main>
  </body>
</html>
