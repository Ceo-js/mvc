<!DOCTYPE html>
<html>
  <head>
    <title><%= pageTitle %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
        gallery {
            display: grid;
            gap: 1.5rem;
        }
        card {
            display: grid;
            width: 150px;
            aspect-ratio: 1 /2.5;
            box-shadow: #0005 0 0 5px;
        }
    </style>
  </head>
  <body>
    <%- include( "./includes/appbar" ) %>
    <main>
        <h1>Products List</h1>


        <% if( products.length ) {%>
            <gallery>
                <% for( let product of products ) { %>
                    <card>
                        <header><img src="<%= product.image %>" alt=""></header>
                        <h1><%= product.title %></h1>
                        <p desc><%= product.description %></p>
                        <p price><%= product.price %></p>
                        <a href="/product/details/<%= product.id %>" btn>Ver Detalhes</a>
                    </card>
                <% } %>
            </gallery>
        <% } else {%>
            <div>No Products Available</div>
        <% } %>
<!-- 
        <% if( products.length ) {%>
            <div>
                <% for( let product of products ) { %>
                    <h3><%= product.title %></h3>
                <% } %>
            </div>
        <% } else {%>
            <div>No Products Available</div>
        <% } %> -->

        home
    </main>
  </body>
</html>
